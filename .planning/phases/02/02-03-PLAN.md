---
plan: 02-03
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/fetch/envelope.rs
  - src/fetch/extract.rs
  - src/fetch/output.rs
  - tests/fetch_envelope_integration.rs
  - tests/fetch_output_modes_integration.rs
  - tests/fetch_file_output_parity_integration.rs
  - .planning/STATE.md
requirements_addressed:
  - EXTR-05
  - OUT-02
  - OUT-03
  - OUT-04
autonomous: false
---

# Plan 02-03: Envelope Finalization, Output Parity, and State Alignment

## Objective

Finalize metadata/output contracts across modes, add explicit stdout-vs-file parity assertions for all output modes, and close planning/state alignment.

## Task List

1. Finalize envelope metadata and output-mode serializers

- Extend envelope and extraction models in `src/fetch/envelope.rs` and `src/fetch/extract.rs` with optional `description` and `canonical_url`.
- Finalize output-mode serialization logic in `src/fetch/output.rs` for default frontmatter markdown, markdown-only, and json-envelope paths.

1. Add explicit OUT-04 byte-parity tests for all output modes

- Add/expand integration coverage in:
  - `tests/fetch_output_modes_integration.rs`
  - `tests/fetch_envelope_integration.rs`
  - `tests/fetch_file_output_parity_integration.rs`
- Assert for each mode (default, `--no-frontmatter`, `--json`) that stdout bytes are exactly equal to bytes written by `-o` for the same input.

1. Checkpoint: execute D2-24 planning/state alignment

- Update `.planning/STATE.md` to mark Phase 2 plan split (`02-01`, `02-02`, `02-03`), waves, and current execution status.
- Verify roadmap/phase state no longer has active-vs-pending ambiguity for Phase 2.
- Pause for human verification of state alignment artifact accuracy before execution proceeds.

## Verification Commands

- `cargo test --test fetch_output_modes_integration`
- `cargo test --test fetch_envelope_integration`
- `cargo test --test fetch_file_output_parity_integration`
- `cargo test --test fetch_file_output_parity_integration parity_default_mode_stdout_equals_file_bytes`
- `cargo test --test fetch_file_output_parity_integration parity_no_frontmatter_mode_stdout_equals_file_bytes`
- `cargo test --test fetch_file_output_parity_integration parity_json_mode_stdout_equals_file_bytes`
- `cargo test`
- `cargo clippy -- -D warnings`

## Acceptance Criteria

- EXTR-05 is satisfied with optional metadata fields populated for HTML and safely nullable for non-HTML.
- OUT-02/OUT-03 serializers are stable and mode-correct.
- OUT-04 includes explicit automated byte-parity assertions for stdout vs `-o` across all output modes.
- D2-24 is completed with an executable state-alignment checkpoint reflected in `.planning/STATE.md`.
